# 複製這個檔案為 .env.local(Vite 會自動讀取,且已被 .gitignore 排除)
# 兩個值都在 Supabase Dashboard → Project Settings → API 頁面可以找到

VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key
